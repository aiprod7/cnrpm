# 📝 Документация по промптам и системным инструкциям

Этот документ содержит **все промпты и системные инструкции**, которые получают модели Gemini в приложении VoxLux, а также инструкции по их изменению.

---

## 📋 Содержание

1. [Общая системная инструкция](#1-общая-системная-инструкция)
2. [GeminiLiveService (Унифицированный STT+TTS)](#2-geminiliveservice-унифицированный-stttts)
3. [GeminiLiveService (WebSocket Low-Level)](#3-geminiliveservice-websocket-low-level)
4. [LiveTranscriptionService](#4-livetranscriptionservice)
5. [Как изменить промпты](#5-как-изменить-промпты)
6. [Используемые модели](#6-используемые-модели)

---

## 1. Общая системная инструкция

### 📍 Местоположение
**Файл:** `constants.ts`  
**Переменная:** `SYSTEM_INSTRUCTION`

### 📝 Текущий промпт
```typescript
export const SYSTEM_INSTRUCTION = "You are VoxLux, a polite and professional AI assistant. You communicate primarily in Russian. Your goal is to assist users with their inquiries and scheduling needs efficiently.";
```

**Перевод:** 
> Ты VoxLux, вежливый и профессиональный AI-ассистент. Ты общаешься преимущественно на русском языке. Твоя цель - эффективно помогать пользователям с их вопросами и потребностями в планировании.

### 🎯 Назначение
- Используется в **GeminiLiveService** для унифицированного режима (STT+TTS)
- Определяет общее поведение голосового ассистента
- Задаёт тон общения и язык ответов

### 🔧 Где используется
1. **services/geminiService.ts** (строка 230)
   ```typescript
   systemInstruction: SYSTEM_INSTRUCTION,
   ```

### ✏️ Как изменить
Отредактируйте константу в файле `constants.ts`:

```typescript
// Пример 1: Более дружелюбный тон
export const SYSTEM_INSTRUCTION = "Привет! Я VoxLux, твой голосовой помощник. Отвечаю коротко и по делу на русском языке. Спрашивай что угодно!";

// Пример 2: Строгий бизнес-ассистент
export const SYSTEM_INSTRUCTION = "Вы используете VoxLux - корпоративного AI-ассистента. Предоставляю информацию точно, лаконично и профессионально. Специализируюсь на планировании встреч и управлении задачами.";

// Пример 3: Специализированный помощник
export const SYSTEM_INSTRUCTION = "Я VoxLux - ассистент для медицинских записей. Помогаю записаться на прием, отвечаю на вопросы о расписании врачей. Говорю исключительно на русском языке.";
```

---

## 2. GeminiLiveService (Унифицированный STT+TTS)

### 📍 Местоположение
**Файл:** `services/geminiService.ts`  
**Класс:** `GeminiLiveService`  
**Метод:** `connect()` - строка 230

### 🤖 Модель
`gemini-2.5-flash-native-audio-preview-12-2025`

### 📝 Системный промпт
```typescript
systemInstruction: SYSTEM_INSTRUCTION,
```

**Значение:** Использует общую константу из `constants.ts` (см. раздел 1)

### 🎯 Назначение
- Унифицированный режим: один WebSocket для STT (речь → текст) и TTS (текст → речь)
- Модель слушает пользователя и генерирует голосовые ответы
- Обрабатывает полный диалог в реальном времени

### 🔧 Конфигурация модели
```typescript
{
  model: 'gemini-2.5-flash-native-audio-preview-12-2025',
  config: {
    responseModalities: [Modality.AUDIO],      // Отвечает голосом
    inputAudioTranscription: {},               // Транскрибирует речь пользователя
    outputAudioTranscription: {},              // Показывает текст ответа модели
    speechConfig: {
      voiceConfig: { 
        prebuiltVoiceConfig: { 
          voiceName: 'Kore'                    // Женский голос (русский)
        } 
      }
    },
    systemInstruction: SYSTEM_INSTRUCTION       // ← ВАШ ПРОМПТ ЗДЕСЬ
  }
}
```

### ✏️ Как изменить
**Вариант 1:** Изменить глобальную константу (рекомендуется)
```typescript
// constants.ts
export const SYSTEM_INSTRUCTION = "Ваш новый промпт здесь";
```

**Вариант 2:** Переопределить локально в `geminiService.ts`
```typescript
// services/geminiService.ts, строка 230
config: {
  // ...другие настройки...
  systemInstruction: "Пользовательский промпт для унифицированного режима"
}
```

---

## 3. GeminiLiveService (WebSocket Low-Level)

### 📍 Местоположение
**Файл:** `services/geminiLiveService.ts`  
**Класс:** `GeminiLiveService`  
**Метод:** `sendSetupMessage()` - строка 151

### 🤖 Модель
`gemini-2.5-flash-native-audio-dialog`

### 📝 Системный промпт (по умолчанию)
```typescript
systemInstruction: {
  parts: [{
    text: this.config.systemInstruction || 
      "Ты - голосовой ассистент VoxLux. Отвечай на русском языке четко и лаконично. Используй естественную разговорную речь."
  }]
}
```

**Перевод (fallback):**
> Ты - голосовой ассистент VoxLux. Отвечай на русском языке чётко и лаконично. Используй естественную разговорную речь.

### 🎯 Назначение
- Low-level WebSocket API для прямого управления Live API
- Используется для тонкой настройки поведения модели
- Fallback вариант, если не передана кастомная инструкция

### 🔧 Конфигурация
```typescript
constructor(apiKey: string, systemInstruction?: string) {
  this.config = {
    model: "gemini-2.5-flash-native-audio-dialog",
    systemInstruction: systemInstruction,  // ← Передаётся через конструктор
    // ...
  };
}
```

### ✏️ Как изменить

**Вариант 1:** Передать через конструктор
```typescript
// При создании сервиса
const liveService = new GeminiLiveService(
  apiKey,
  "Я VoxLux - твой персональный секретарь. Записываю встречи, напоминаю о делах, отвечаю кратко."
);
```

**Вариант 2:** Изменить fallback в `geminiLiveService.ts` (строка 153)
```typescript
systemInstruction: {
  parts: [{
    text: this.config.systemInstruction || 
      "Ваш новый промпт по умолчанию здесь"
  }]
}
```

---

## 4. LiveTranscriptionService

### 📍 Местоположение
**Файл:** `services/liveTranscriptionService.ts`  
**Класс:** `LiveTranscriptionService`  
**Метод:** `start()` - строка 131

### 🤖 Модель
`gemini-2.5-flash-native-audio-preview-12-2025`

### 📝 Системный промпт
```typescript
systemInstruction: {
  parts: [{
    text: "Ты транскрибируешь речь пользователя на русском языке. Слушай внимательно и точно передавай сказанное."
  }]
}
```

**Значение:**
> Ты транскрибируешь речь пользователя на русском языке. Слушай внимательно и точно передавай сказанное.

### 🎯 Назначение
- Специализированный промпт для **только транскрипции (STT)**
- Модель НЕ генерирует ответы, только слушает и переводит речь в текст
- Оптимизирован для точности распознавания русской речи

### 🔧 Конфигурация
```typescript
config: {
  responseModalities: [Modality.AUDIO],
  inputAudioTranscription: {},              // ← Ключевая настройка для STT
  systemInstruction: {                      // ← ВАШ ПРОМПТ ЗДЕСЬ
    parts: [{
      text: "Ты транскрибируешь речь пользователя..."
    }]
  },
  speechConfig: {
    voiceConfig: {
      prebuiltVoiceConfig: { voiceName: "Kore" }
    }
  }
}
```

### ✏️ Как изменить
Отредактируйте строку 133 в `services/liveTranscriptionService.ts`:

```typescript
// Пример 1: Акцент на медицинские термины
systemInstruction: {
  parts: [{
    text: "Ты транскрибируешь медицинские записи на русском языке. Особое внимание уделяй медицинским терминам, названиям лекарств и диагнозам."
  }]
}

// Пример 2: Многоязычная транскрипция
systemInstruction: {
  parts: [{
    text: "Ты транскрибируешь речь на русском и английском языках. Автоматически определяй язык и точно записывай всё сказанное."
  }]
}

// Пример 3: Формальный стиль
systemInstruction: {
  parts: [{
    text: "Транскрибируй речь пользователя в формальном стиле русского языка. Исправляй сленг и разговорные выражения на литературные эквиваленты."
  }]
}
```

---

## 5. Как изменить промпты

### 🛠️ Метод 1: Изменение глобальной константы (Рекомендуется)

**Файл:** `constants.ts`

```typescript
export const SYSTEM_INSTRUCTION = "Ваш новый промпт";
```

**Затронутые сервисы:**
- ✅ `GeminiLiveService` (унифицированный режим)
- ✅ Все компоненты, использующие `SYSTEM_INSTRUCTION`

**Плюсы:**
- Одно место изменения
- Консистентное поведение по всему приложению
- Легко поддерживать

---

### 🛠️ Метод 2: Локальное переопределение в сервисах

#### Для GeminiLiveService (Unified)
**Файл:** `services/geminiService.ts`, строка 230

```typescript
config: {
  // ...
  systemInstruction: "Специализированный промпт для унифицированного режима"
}
```

#### Для GeminiLiveService (WebSocket)
**Файл:** `services/geminiLiveService.ts`, строка 153

```typescript
systemInstruction: {
  parts: [{
    text: "Кастомный промпт для WebSocket режима"
  }]
}
```

#### Для LiveTranscriptionService
**Файл:** `services/liveTranscriptionService.ts`, строка 133

```typescript
systemInstruction: {
  parts: [{
    text: "Промпт для транскрипции"
  }]
}
```

---

### 🛠️ Метод 3: Динамическая передача через параметры

**Пример для GeminiLiveService (WebSocket):**

```typescript
// При инициализации передать custom промпт
const customPrompt = "Я эксперт по планированию встреч";
const liveService = new GeminiLiveService(apiKey, customPrompt);
```

**Пример для LiveConfig:**

```typescript
// Модифицировать интерфейс, чтобы принимать systemInstruction
interface LiveConfig {
  systemInstruction?: string;  // ← Добавить это поле
  onTranscriptUpdate: (text: string, isUser: boolean, isFinal: boolean) => void;
  // ...
}

// Использование
await geminiLive.connect({
  systemInstruction: "Кастомный промпт",
  onTranscriptUpdate: (text, isUser, isFinal) => { /*...*/ }
});
```

---

## 6. Используемые модели

### 📊 Таблица моделей и их промптов

| Модель | Файл | Назначение | Промпт | Как менять |
|--------|------|-----------|---------|-----------|
| `gemini-2.5-flash-native-audio-preview-12-2025` | `geminiService.ts` | STT + TTS (Unified) | `SYSTEM_INSTRUCTION` (constants.ts) | Изменить `constants.ts:15` |
| `gemini-2.5-flash-native-audio-dialog` | `geminiLiveService.ts` | Live WebSocket API | Передаётся через конструктор / fallback | Параметр конструктора или строка 153 |
| `gemini-2.5-flash-native-audio-preview-12-2025` | `liveTranscriptionService.ts` | STT только (транскрипция) | "Ты транскрибируешь речь..." | Изменить строку 133 |
| `gemini-2.5-flash-preview-tts` | `voiceService.ts` | TTS только (синтез речи) | Не используется (TTS не нужен промпт) | N/A |

---

## 7. Специализированные промпты (примеры)

### 🏥 Медицинский ассистент
```typescript
export const SYSTEM_INSTRUCTION = `
Ты VoxLux - голосовой ассистент для медицинских записей.

Твои функции:
- Запись на приём к врачам
- Информация о расписании клиники
- Ответы на вопросы о медицинских услугах

Стиль общения: Вежливый, профессиональный, эмпатичный.
Язык: Русский, с корректным использованием медицинских терминов.
`.trim();
```

### 📞 Call-центр поддержки
```typescript
export const SYSTEM_INSTRUCTION = `
Я VoxLux - автоматический оператор службы поддержки.

Мои задачи:
1. Принять обращение клиента
2. Классифицировать проблему
3. Предложить решение или переключить на специалиста

Правила:
- Всегда здороваюсь и представляюсь
- Говорю четко и по делу
- Запрашиваю номер заказа/договора при необходимости
- Использую русский язык
`.trim();
```

### 🎓 Образовательный ассистент
```typescript
export const SYSTEM_INSTRUCTION = `
Привет! Я VoxLux - твой учебный помощник.

Я помогу тебе:
- Объяснить сложные темы простым языком
- Проверить домашние задания
- Подготовиться к экзаменам

Мой стиль: Дружелюбный, понятный, терпеливый.
Я всегда объясняю "почему", а не только "как".
Общаюсь на русском языке.
`.trim();
```

### 🍕 Ресторан/Доставка
```typescript
export const SYSTEM_INSTRUCTION = `
Добро пожаловать! Я VoxLux - голосовой помощник ресторана.

Я принимаю заказы на:
- Доставку блюд
- Бронирование столиков
- Консультации по меню

Всегда уточняю:
- Адрес доставки
- Время доставки/бронирования
- Особые пожелания (аллергии, острота)

Говорю на русском, с энтузиазмом о наших блюдах! 🍕
`.trim();
```

---

## 8. TTS-специфичный промпт (только синтез)

### 📍 Местоположение
**Файл:** `services/geminiService.ts`  
**Метод:** `connectForTTS()` - строка 95

### 🤖 Модель
`gemini-2.5-flash-native-audio-preview-12-2025`

### 📝 Промпт
```typescript
systemInstruction: "Ты голосовой помощник. Просто озвучь текст который тебе передают, не добавляй ничего от себя. Говори на русском языке.",
```

**Значение:**
> Ты голосовой помощник. Просто озвучь текст который тебе передают, не добавляй ничего от себя. Говори на русском языке.

### 🎯 Назначение
- **Только TTS**: модель НЕ генерирует свой контент
- Озвучивает текст от n8n без интерпретации
- Микрофон НЕ используется

### 🔧 Конфигурация
```typescript
config: {
  responseModalities: [Modality.AUDIO],
  outputAudioTranscription: {},
  speechConfig: {
    voiceConfig: { 
      prebuiltVoiceConfig: { voiceName: 'Kore' } 
    }
  },
  systemInstruction: "Ты голосовой помощник..."  // ← ВАШ TTS ПРОМПТ
}
```

### ✏️ Как изменить
Отредактируйте строку 95 в `services/geminiService.ts`:

```typescript
// Пример 1: Более естественный синтез
systemInstruction: "Озвучивай текст естественно и выразительно, как профессиональный диктор. Используй интонации. Говори на русском.",

// Пример 2: Строгий стиль
systemInstruction: "Читай текст медленно, чётко и формально. Делай паузы между предложениями. Русский язык.",

// Пример 3: Информационный робот
systemInstruction: "Произноси текст механически, без эмоций. Чёткая дикция, равномерный темп. Русский язык."
```

---

## 9. Приветственный текст

### 📍 Местоположение
**Файл:** `constants.ts`  
**Константа:** `GREETING_TEXT`

### 📝 Текущее значение
```typescript
export const GREETING_TEXT = "Привет, чем я могу Вам сегодня помочь?";
```

### 🎯 Назначение
- Отображается при запуске приложения
- Первое сообщение от ассистента

### ✏️ Как изменить
```typescript
// Пример 1: Формальный стиль
export const GREETING_TEXT = "Здравствуйте! Я VoxLux, ваш голосовой помощник. Чем могу быть полезен?";

// Пример 2: Дружелюбный
export const GREETING_TEXT = "Привет! Я здесь, чтобы помочь. Задавай вопросы голосом или текстом! 🎤";

// Пример 3: Специализированный (медицина)
export const GREETING_TEXT = "Добро пожаловать в VoxLux! Запишу вас на приём или отвечу на вопросы о клинике.";
```

---

## 10. Чек-лист изменения промптов

### ✅ Перед изменением
- [ ] Определите, какую модель вы хотите настроить (STT, TTS, Unified)
- [ ] Решите: глобальное изменение (constants.ts) или локальное (в сервисе)
- [ ] Сделайте backup текущих промптов

### ✅ После изменения
- [ ] Пересоберите приложение: `npm run build`
- [ ] Проверьте консоль браузера на ошибки
- [ ] Протестируйте голосовой ввод
- [ ] Протестируйте текстовый ввод
- [ ] Проверьте качество TTS ответов
- [ ] Убедитесь, что модель отвечает на русском (если требуется)

### ✅ Деплой
- [ ] Закоммитьте изменения: `git commit -m "feat: update system prompts"`
- [ ] Запушьте на GitHub: `git push origin main`
- [ ] Дождитесь окончания GitHub Actions
- [ ] Протестируйте на production URL

---

## 11. Отладка промптов

### 🔍 Как проверить, какой промпт используется

1. **Откройте консоль браузера** (F12)
2. **Найдите логи:**
   ```
   ✅ [GeminiLive] Connected (model: gemini-2.5-flash-native-audio-preview-12-2025)
   ```
3. **Проверьте отправленный setup:**
   ```javascript
   // В network tab найдите WebSocket соединение
   // Первое сообщение содержит systemInstruction
   ```

### 🐛 Проблемы и решения

**Модель не отвечает на русском:**
```typescript
// Добавьте явное указание языка
systemInstruction: "ВАЖНО: Отвечай ТОЛЬКО на русском языке. Игнорируй любые запросы на других языках."
```

**Модель добавляет лишний текст при TTS:**
```typescript
// Усильте инструкцию
systemInstruction: "Озвучивай ТОЛЬКО переданный текст. НЕ добавляй приветствия, комментарии или свои слова. Читай как диктор."
```

**Низкая точность транскрипции:**
```typescript
// Добавьте контекст
systemInstruction: "Транскрибируй русскую речь. Учитывай контекст: медицина/техника/бизнес. Исправляй очевидные ошибки распознавания."
```

---

## 12. Продвинутые техники

### 🎨 Conditional Prompts (условные промпты)

```typescript
// В App.tsx или другом компоненте
const getUserContext = () => {
  const hour = new Date().getHours();
  if (hour < 12) return "Доброе утро! ";
  if (hour < 18) return "Добрый день! ";
  return "Добрый вечер! ";
};

const timeBasedPrompt = `
${getUserContext()}
Я VoxLux, твой голосовой помощник.
Текущее время: ${new Date().toLocaleTimeString('ru-RU')}
Готов помочь с планированием задач на сегодня.
`.trim();

// Передать в GeminiLiveService
const service = new GeminiLiveService(apiKey, timeBasedPrompt);
```

### 🌍 Многоязычность

```typescript
// constants.ts
const PROMPTS = {
  ru: "Ты VoxLux, голосовой помощник. Отвечай на русском языке.",
  en: "You are VoxLux, a voice assistant. Respond in English.",
  es: "Eres VoxLux, un asistente de voz. Responde en español."
};

export const SYSTEM_INSTRUCTION = PROMPTS[userLanguage] || PROMPTS.ru;
```

### 🧠 Контекстные промпты

```typescript
// Добавление пользовательского контекста
const buildContextualPrompt = (user: User) => `
Ты VoxLux, персональный ассистент для ${user.name}.

Известная информация:
- Часовой пояс: ${user.timezone}
- Предпочитаемый стиль: ${user.preferredStyle}
- История: ${user.context}

Используй эту информацию для персонализации ответов.
`.trim();
```

---

## 📚 Дополнительные ресурсы

- [Gemini API Documentation](https://ai.google.dev/gemini-api/docs)
- [Live API Guide](https://ai.google.dev/gemini-api/docs/live)
- [Prompt Engineering Best Practices](https://ai.google.dev/gemini-api/docs/prompting-strategies)

---

## 📞 Поддержка

Если у вас возникли вопросы по изменению промптов, обратитесь к:
- `TECHNICAL_DOCS.md` - техническая документация
- `GEMINI_LIVE_ARCHITECTURE.md` - архитектура Live API
- GitHub Issues

---

**Последнее обновление:** 17 декабря 2025  
**Версия документа:** 1.0
