# 🎯 Quick Start: Unified Gemini Live Architecture

## ✅ Что сделано

### 1. Создана резервная точка
```bash
Tag: backup-before-gemini-live (commit a4c816d)
```

### 2. Реализована унифицированная архитектура
**Файл**: `services/geminiService.ts`
- **Модель**: `gemini-2.5-flash-native-audio-preview-12-2025`
- **Функционал**: STT + TTS в одном WebSocket соединении
- **Преимущества**:
  - Задержка ~500ms (было 1-2s)
  - Поддержка прерывания (barge-in)
  - Real-time streaming для STT и TTS

### 3. Создана документация
- **GEMINI_LIVE_ARCHITECTURE.md** - подробная техническая документация
- **ROLLBACK_UNIFIED.md** - инструкции по откату
- **package.json** - версия `1.5.0-unified`

---

## 🚀 Как это работает

### Архитектура

```
Пользователь говорит
      ↓
  Микрофон (PCM 16kHz)
      ↓
  WebSocket → Gemini Live API
      ↓
┌─────────────────────────┐
│  Unified Model          │
│  gemini-2.5-flash-...   │
│                         │
│  1. STT: "привет"       │ ← Транскрибирует речь
│  2. AI: Думает          │
│  3. TTS: Audio + Text   │ ← Генерирует ответ
└─────────────────────────┘
      ↓
  Динамики (PCM 24kHz)
```

### Ключевые особенности

1. **Single Connection**
   - Открывается 1 WebSocket
   - Работает весь разговор
   - Не нужно переключаться между моделями

2. **Real-time Streaming**
   ```typescript
   onTranscriptUpdate: (text, isUser, isFinal) => {
     // isUser = true: STT (пользователь говорит)
     // isUser = false: TTS (AI отвечает)
     // isFinal = true: фраза закончена
   }
   ```

3. **Interruption Support**
   - Пользователь может перебить AI
   - Автоматическая остановка воспроизведения
   - Модель начинает слушать новый запрос

---

## 📝 Примеры кода

### Пример 1: Подключение

```typescript
import { geminiService } from './services/geminiService';

const startConversation = async () => {
  await geminiService.connect({
    onTranscriptUpdate: (text, isUser, isFinal) => {
      console.log(isUser ? 'User:' : 'AI:', text);
      
      if (isFinal) {
        // Добавить сообщение в чат
        addMessage({ role: isUser ? 'user' : 'model', text });
      }
    },
    onClose: () => console.log('Disconnected'),
    onError: (err) => console.error('Error:', err)
  });
};
```

### Пример 2: Остановка

```typescript
const stopConversation = async () => {
  await geminiService.disconnect();
};
```

### Пример 3: Визуализатор

```typescript
const analyser = geminiService.getAnalyserNode();
<Visualizer analyser={analyser} state={appState} />
```

---

## 🧪 Тестирование

### 1. Проверить консоль

```
✅ Должны быть логи:
🔌 [GeminiLive] Connecting to unified model...
🔊 [GeminiLive] Audio contexts ready (Input: 16kHz, Output: 24kHz)
🎤 [GeminiLive] Microphone access granted
✅ [GeminiLive] Connected (model: gemini-2.5-flash-native-audio-preview-12-2025)
📤 [GeminiLive] Sent 10 audio chunks
📝 [GeminiLive] STT: "привет"
🔊 [GeminiLive] Playing audio chunk (2.34s)
💬 [GeminiLive] TTS Text: "Здравствуйте!"
✅ [GeminiLive] Turn complete
```

### 2. Проверить функционал

- [ ] Нажать "Start Live" - должно подключиться
- [ ] Говорить - видеть транскрипт в реальном времени
- [ ] Дождаться ответа - услышать голос AI
- [ ] Перебить AI во время ответа - должна остановиться
- [ ] Нажать "Stop" - должно отключиться

### 3. Проверить ошибки

**Если модель недоступна**:
```
❌ [GeminiLive] Error: Model not found
→ Откатиться к v1.4.1
```

**Если нет доступа к микрофону**:
```
❌ [GeminiLive] Error: Permission denied
→ Разрешить доступ в браузере
```

---

## 🔄 Откат к стабильной версии

### Быстрый откат

```bash
cd e:\tg_mini_apps\voxlux
git reset --hard backup-before-gemini-live
git push origin gemini --force
```

### Что вернётся

- **Версия**: 1.4.1 (stable)
- **STT**: `liveTranscriptionService.ts` (отдельный сервис)
- **TTS**: `voiceService.ts` (отдельный сервис)
- **Модели**: 
  - STT: `gemini-2.5-flash-native-audio-preview-12-2025`
  - TTS: `gemini-2.5-flash-preview-tts`

**Детали**: См. `ROLLBACK_UNIFIED.md`

---

## 📊 Сравнение версий

| Параметр | v1.4.1 (Stable) | v1.5.0 (Unified) |
|----------|-----------------|------------------|
| **Подключение** | 2 сервиса | 1 сервис |
| **Модели** | 2 модели | 1 модель |
| **Задержка STT** | ~100-300ms | ~100-300ms |
| **Задержка TTS** | ~800ms | ~400ms (streaming) |
| **Общая задержка** | ~1-2s | ~500-700ms |
| **Прерывание** | ❌ Нет | ✅ Есть |
| **Real-time** | Только STT | STT + TTS |
| **Статус** | ✅ Stable | 🧪 Experimental |

---

## 📚 Документация

### Основные файлы

- **GEMINI_LIVE_ARCHITECTURE.md** - Полная техническая документация
  - Архитектура и flow
  - Примеры кода
  - Спецификации аудио
  - Обработка ошибок

- **ROLLBACK_UNIFIED.md** - Инструкции по откату
  - 3 метода отката
  - Чеклист тестирования
  - Troubleshooting

- **services/geminiService.ts** - Код сервиса
  - 400+ строк с комментариями
  - Обработка STT/TTS
  - Управление аудио потоками

---

## 🎯 Следующие шаги

1. **Тестирование**
   - Запустить в Telegram Mini Apps
   - Проверить STT (распознавание речи)
   - Проверить TTS (голос AI)
   - Проверить прерывание

2. **Если работает**
   - Обновить версию до `1.5.0` (убрать `-unified`)
   - Добавить в README.md
   - Обновить STT_ANALYSIS.md

3. **Если НЕ работает**
   - Выполнить откат: `git reset --hard backup-before-gemini-live`
   - Документировать ошибки
   - Вернуться к v1.4.1 (stable)

---

## 📞 Поддержка

**Резервная точка**: `backup-before-gemini-live` (commit a4c816d)  
**Текущая версия**: 1.5.0-unified (commit 6dcf36c)  
**Дата**: 17 декабря 2025

**Команда для отката**:
```bash
git reset --hard backup-before-gemini-live && git push origin gemini --force
```

---

**Готово к тестированию! 🚀**
