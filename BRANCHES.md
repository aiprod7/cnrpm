# 🌿 Структура веток проекта VoxLux

## Обзор

Проект VoxLux использует несколько веток для разделения разных реализаций голосового движка и экспериментальных функций.

---

## 📋 Активные ветки

### 🔷 `main` - OpenRouter API версия

**Статус**: ✅ Активная (продакшн)  
**Последний коммит**: `86134eb` - "fix: rollup build error + show branch name in header"

#### Особенности:
- **STT (Speech-to-Text)**: OpenRouter API с моделью `google/gemini-2.5-flash`
- **TTS (Text-to-Speech)**: Web Speech API (браузерный синтезатор)
- **API ключ**: `OPENROUTER_API_KEY` (через переменные окружения)
- **Зависимости**: Не использует `@google/genai` SDK

#### Архитектура:
```
services/voiceService.ts
├── STT: fetch → https://openrouter.ai/api/v1/chat/completions
├── TTS: window.speechSynthesis (браузерный API)
└── Аудио: Web Audio API для визуализации
```

#### Когда использовать:
- ✅ Для продакшн-деплоя с ограничениями на использование Google SDK
- ✅ Для кросс-платформенной совместимости
- ✅ Когда нужна гибкость выбора моделей через OpenRouter

#### Деплой:
Автоматический через GitHub Actions при push в `main`:
```yaml
on:
  push:
    branches: [main]
```

---

### 🔶 `gemini` - Google Gemini SDK версия

**Статус**: ✅ Активная (разработка)  
**Последний коммит**: `7b7e6b6` - "docs: Update README with Gemini TTS documentation and API key setup instructions"  
**Версия**: `v1.0.0`

#### Особенности:
- **STT (Speech-to-Text)**: Google Gemini Audio Understanding + Web Speech API (fallback)
- **TTS (Text-to-Speech)**: **Gemini 2.5 Flash Preview TTS** с нативным синтезом речи
- **Голос**: `Kore` (женский, чёткий)
- **API ключ**: `DEFAULT_GEMINI_API_KEY` или `GEMINI_API_KEY`
- **Зависимости**: `@google/genai` SDK v1.33.0

#### Архитектура:
```
services/voiceService.ts
├── STT: Web Speech API → Gemini Audio (WAV format)
├── TTS: GoogleGenAI.models.generateContent()
│   ├── model: "gemini-2.5-flash-preview-tts"
│   ├── responseModalities: [Modality.AUDIO]
│   └── voiceConfig: { voiceName: 'Kore' }
└── Аудио: Web Audio API (PCM 24kHz декодирование)
```

#### Когда использовать:
- ✅ **Для лучшего качества TTS** (естественная речь, эмоции, акценты)
- ✅ Для продакшна с официальным Google Gemini SDK
- ✅ Когда важна поддержка 24 языков и 30 голосов
- ✅ Для экспериментов с подсказками (style, accent, pacing)

#### Преимущества:
- 🎯 Управляемая генерация речи (можно задавать стиль, акцент, темп)
- 🌍 Поддержка 24 языков (включая русский `ru-RU`)
- 🎤 30 вариантов голосов (Zephyr, Puck, Kore, Fenrir и др.)
- 📝 Длинный контекст: 32K токенов

#### Деплой:
Автоматический через GitHub Actions при push в `gemini` (если настроен).

---

### 🔸 `refs/pull/2/head` - Pull Request #2

**Статус**: 🔄 Pull Request (закрыт или объединён)  
**Тип**: Временная ветка для PR

#### Что это:
Это не обычная ветка, а **ссылка на Pull Request #2**. GitHub создаёт такие refs автоматически для каждого PR.

#### Структура:
```
refs/pull/2/head    → HEAD коммит PR (последние изменения в PR)
refs/pull/2/merge   → Результат слияния PR с целевой веткой (тестовый)
```

#### Как посмотреть:
```bash
# Посмотреть PR #2
git fetch origin pull/2/head:pr-2
git checkout pr-2

# Или онлайн
https://github.com/aiprod7/cnrpm/pull/2
```

#### Когда появляется:
- При создании Pull Request (например, из `gemini` в `main`)
- GitHub Actions может тестировать PR перед слиянием
- После merge PR, ветка `refs/pull/2/head` остаётся в истории

---

## 📊 Сравнение веток

| Параметр | `main` | `gemini` |
|----------|--------|----------|
| **STT** | OpenRouter API | Web Speech + Gemini Audio |
| **TTS** | Web Speech API | Gemini 2.5 Flash TTS |
| **SDK** | Нет | @google/genai v1.33.0 |
| **Качество TTS** | ⭐⭐⭐ (браузерное) | ⭐⭐⭐⭐⭐ (AI-генерация) |
| **Голоса** | 1-2 системных | 30 AI голосов |
| **Языки TTS** | ~10-15 | 24 языка |
| **Управление стилем** | ❌ Нет | ✅ Да (style, accent, pacing) |
| **API ключ** | OPENROUTER_API_KEY | DEFAULT_GEMINI_API_KEY |
| **Версия** | 0.x.x | 1.0.0 |

---

## 🔄 Workflow разработки

### Типичный процесс:

1. **Новая фича** → создать ветку от `gemini` или `main`
   ```bash
   git checkout gemini
   git checkout -b feature/new-voice
   ```

2. **Тестирование** → локальный dev server
   ```bash
   npm run dev
   ```

3. **Pull Request** → создать PR в `gemini` или `main`
   ```bash
   git push origin feature/new-voice
   # Создать PR на GitHub
   ```

4. **Review & Merge** → после проверки слить в целевую ветку

5. **Деплой** → автоматически через GitHub Actions

---

## 🚀 Быстрый переход между ветками

```bash
# Переключиться на OpenRouter версию
git checkout main
npm install
npm run dev

# Переключиться на Gemini версию
git checkout gemini
npm install
npm run dev
```

---

## 📦 Версионирование

### Семантическое версионирование (SemVer)

Формат: `MAJOR.MINOR.PATCH`

- **MAJOR** (1.x.x) - несовместимые изменения API
- **MINOR** (x.1.x) - новые функции (обратно совместимые)
- **PATCH** (x.x.1) - исправления багов

### Текущие версии:

- `main`: `0.5.0` (OpenRouter, стабильная)
- `gemini`: `1.0.0` (Gemini SDK, новая архитектура)

### Как обновить версию:

1. Изменить в `package.json`:
   ```json
   "version": "1.1.0"
   ```

2. Обновить в `vite.config.ts`:
   ```typescript
   'process.env.APP_VERSION': JSON.stringify('1.1.0')
   ```

3. Закоммитить:
   ```bash
   git add package.json vite.config.ts
   git commit -m "chore: bump version to 1.1.0"
   git push
   ```

---

## 🎯 Рекомендации

### Для продакшн-деплоя:
- ✅ Используйте `gemini` для лучшего качества TTS
- ✅ Настройте `DEFAULT_GEMINI_API_KEY` в GitHub Secrets
- ✅ Тестируйте локально перед push

### Для экспериментов:
- 🧪 Создавайте feature-ветки от `gemini`
- 🧪 Используйте `main` для fallback на OpenRouter

### Для поддержки обеих версий:
- 🔀 Держите обе ветки (`main` и `gemini`) актуальными
- 🔀 Синхронизируйте общие изменения (UI, компоненты) между ветками

---

## 📞 Поддержка

Если возникли вопросы по веткам:
- Посмотрите историю: `git log --oneline --graph --all`
- Сравните ветки: `git diff main..gemini`
- Проверьте PR: https://github.com/aiprod7/cnrpm/pulls

---

**Последнее обновление**: 16 декабря 2025
